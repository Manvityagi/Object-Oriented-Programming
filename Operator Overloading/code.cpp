#include <bits/stdc++.h>
using namespace std;

class Fraction
{
    int num;
    int deno;
public:
    Fraction(int num, int deno): num(num), deno(deno); 
    
    Fraction operator+(Fraction f2){
        Fraction ans(1,1);
        
    }
};

int main()
{
    return 0;
}